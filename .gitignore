target/
**/*.rs.bk

# Cargo.lock is committed on purpose. The old advice was to ignore it in
# libraries; current Cargo guidance is to commit it so CI resolves the same
# versions every run. It does not constrain downstream consumers either way.

__pycache__/
.pytest_cache/
.mypy_cache/
*.py[cod]
.venv/
# maturin develop drops the built extension into the source tree
*.so
*.pyd
*.pdb
*.dll
dist/
*.egg-info/

node_modules/
*.node

# Written by `napi create-npm-dirs` on every build and publish.
seam-js/npm/

.vscode/
.idea/
*.bak
